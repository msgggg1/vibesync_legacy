package com.vibesync.page.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/page")
public class PageApiController {

    /*
	@GetMapping("/user")
    public ResponseEntity<UserPageDTO> user(@RequestParam("acIdx") int acIdx) {
    	UserPageDTO userPageData = pageService.getUserPageData(acIdx);
    	return new ResponseEntity<>(userPageData, HttpStatus.OK);
    }
    */
	
	/*
    @GetMapping("/main")
    public ResponseEntity<MainPageDTO> main() {
        // ... 메인 페이지 데이터 조회 로직
    }
	*/
}
