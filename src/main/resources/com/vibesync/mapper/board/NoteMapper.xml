<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibesync.board.mapper.NoteMapper">

<!-- 게시글 목록 조회 -->
<!-- public List<NoteVO> selectAll(); -->
	<select id="selectAll" resultType="NoteVO">
		SELECT n.note_idx, n.title, ua.nickname AS author_name
		FROM note n
		JOIN userPage up ON n.userPg_idx = up.userPg_idx
		JOIN userAccount ua ON up.ac_idx = ua.ac_idx
	</select>
<!-- // -->

</mapper>