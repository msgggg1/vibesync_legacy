<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibesync.common.mapper.ContentMapper">

<!-- 전체 컨텐츠 목록 조회 -->
<!-- public List<ContentVO> selectAll(); -->
	<select id="selectAll" resultType="ContentVO">
		<![CDATA[
		SELECT
			content_idx, title, img, dsc, category_idx
		FROM
			contents
		]]>
	</select>
<!-- // -->

</mapper>